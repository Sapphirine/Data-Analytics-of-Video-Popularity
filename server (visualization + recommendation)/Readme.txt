The server Python program has dependency on Flask and PyMongo.
If you have installed these packages and imported data into MongoDB, you can run the server program by entering the following command:

sudo python app1.py

Then you can visit the the websites using the link: http://0.0.0.0